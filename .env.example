# CSE MCP Server Environment Variables
# Copy this file to .env and fill in your values

# Optional: Add any API keys or authentication tokens if needed in the future
# API_KEY=your_api_key_here

# Optional: Set the API base URL (defaults to https://www.cse.lk/api)
# API_BASE_URL=https://www.cse.lk/api

# Optional: Set cache TTL in milliseconds (defaults to 30000 = 30 seconds)
# CACHE_TTL=30000
